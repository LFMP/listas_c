int main()
{
/*
	I = I +1 -> Válido porém como o valor de  "I" não foi 
				setado previamente, 
				não há garantias que "I" será igual 
				a zero para primeira soma;
	X = −X -> 	Válido, "X" irá assumir o valor oposto daquele
				guardado na memória;
	−X = X ->	Inválido, o utilização do caracter "-" no nome
				da variável é inválido;
	A=B+A ->	Válido desde que "B" já tenha um sido declarado;
	B+A=A ->	Inválido, o utilização do caracter "-" no nome
				da variável é inválido;
	RAIZ1 = −B / 2∗A -> Válido, não há restrições quanto a realização
						de operações como valor da variável;
	RAIZ1 = −B / (2∗A) ->Válido, não há restrições quanto a realização
						de operações como valor da variável;
*/
}
